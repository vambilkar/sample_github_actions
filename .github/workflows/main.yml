name: "Hello world "
on:
 push:
   branches:
     -master
     -production
jobs:
   hello-world:
   runs-on: windows-latest
steps:
    - name: "print hello world:
     run: echo "hello world"
     
   
